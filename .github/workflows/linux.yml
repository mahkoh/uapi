name: Linux

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install
      run: curl https://sh.rustup.rs -sSf | sh -s -- -y --default-toolchain stable --profile=minimal
    - name: Build
      run: |
        . $HOME/.cargo/env
        sudo -E cargo build --verbose
    - name: Run tests
      run: |
        . $HOME/.cargo/env
        sudo -E cargo test --verbose
